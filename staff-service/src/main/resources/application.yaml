spring:
  application:
    name: ${SPRING_APPLICATION_NAME}
  datasource:
    url: jdbc:mysql://hrmis-aiven-instance-hrmis.h.aivencloud.com:24695/staff_db?ssl-mode=REQUIRED
    username: avnadmin
    password: AVNS_CVMazlms80CpHegeIha
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  config:
    import: "${SPRING_CONFIG_IMPORT}"
